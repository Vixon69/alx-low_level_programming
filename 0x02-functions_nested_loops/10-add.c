#include "main.h"

/**
 * add - add 2 intergers
 *
 * @n: ascii value
 * @j: ascii value
 *
 * Return: Always 0 (success)
 */
int add(int n, int j)
{
	int sum;

	sum = n + j;

	if (n != -n)
	{
		return (sum);
	}
	else
	{
		return (sum);
	}
}
