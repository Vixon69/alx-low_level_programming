#include "main.h"
/**
 * times_table - prints out 9 times
 *
 * Return: void
 */
void times_table(void)
{
	int row, column, tens, ones;

	for (row = 0; row <= 9; row++)
	{
		for (column = 0; column <= 9; column++)
		{
			product = row * column;
			tens = product / 10;
			ones = product % 10;

			if (column
}
