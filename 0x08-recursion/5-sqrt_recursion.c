#include "main.h"
/**
 * _sqrt_recursion - returns the natural sqaure root of a number
 * @n: the value to be squared
 * Return: he square root
 */

int _sqrt_recursion(int n)
{
	if (n == 0)
		return (-1);
	else if (n == 1)
		return (1);
	else if (n % 0.5 = 0)
		return (
}
